export default function AddressTitle({title}) {
  return (
    <h2
      className="text-base uppercase tracking-tight opacity-60 pt-5 pb-5
    md:pb-7
    lg:pb-10.5"
    >
      {title}
    </h2>
  );
}
